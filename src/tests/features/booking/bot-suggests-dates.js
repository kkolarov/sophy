'use strict';

let chai = require('chai');
let expect = chai.expect;
let sinon = require('sinon');

describe("A bot has an intent to suggests dates", () => {

  context("with the assumption that a client's request is invalid", () => {

  });

  context("with the assumption that a client's request is valid", () => {

  });
});
